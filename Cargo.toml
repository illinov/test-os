[workspace]
resolver = "3"
default-members = ["kernel"]
members = ["bootloader","kernel"]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[workspace.dependencies]
bootloader = { path = "bootloader" }
